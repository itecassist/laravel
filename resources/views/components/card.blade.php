
<div {{ $attributes->merge(['class' => 'card']) }}>
    <div class="card-header h5 text-indigo-400">{{ $header }}</div>
    <div class="card-body">{{ $body }}</div>
    <div class="modal-footer">{{ $footer }}</div>
</div>
